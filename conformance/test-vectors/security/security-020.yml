id: security-020
description: "JSON with deeply nested objects at top level (not in ext: field) - rejected by schema"
level: 3
spec_clause: "Section 12.1, Section 12.11"
input_type: generated
generate:
  type: deeply_nested_field
  field: "unknown_field"
  depth: 200
base_input:
  specversion: "1.0"
  id: "cs_sec_020"
  vendor_id: "acme"
  category: "informational"
  severity: "informational"
  title: "Deep nesting in unknown field"
  summary: "An unknown non-ext field with 200 levels of nesting. Must be rejected by schema and not cause stack overflow."
  published_at: "2026-04-10T14:00:00Z"
  source_type: "crawled"
expected:
  valid: false
  no_crash: true
  max_memory_mb: 50
  max_time_ms: 5000
  reason: "unknown_field is not a defined ChangeSpec field and not an ext: prefixed extension field. The schema rejects it via additionalProperties: false before deep nesting is processed."
notes: "The schema's additionalProperties: false rejects the unknown field. The deeply nested value must not cause a stack overflow during JSON parsing (before schema validation). Validators must enforce parse-time depth limits per Section 12.11."
