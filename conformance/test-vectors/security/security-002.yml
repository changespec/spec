id: security-002
description: SSRF risk - source_url with http:// scheme rejected by https-only requirement
level: 3
spec_clause: "Section 1.3, Section 12.10"
input:
  specversion: "1.0"
  id: "cs_sec_002"
  vendor_id: "acme"
  category: "informational"
  severity: "informational"
  title: "SSRF test - http localhost URL"
  summary: "source_url points to localhost using http://. Must be rejected: schema requires https:// scheme for all URL fields."
  published_at: "2026-04-10T14:00:00Z"
  source_type: "crawled"
  source_url: "http://127.0.0.1/secret"
expected:
  valid: false
  reason: "source_url must use the https:// scheme per Section 1.3 and the schema pattern constraint. http:// is not permitted."
notes: "ChangeSpec 1.0 restricts source_url and migration_url to https:// via schema pattern. http:// URLs are rejected at the schema level, not just at fetch time. This provides protection against both SSRF (private IP) and downgrade attacks."
